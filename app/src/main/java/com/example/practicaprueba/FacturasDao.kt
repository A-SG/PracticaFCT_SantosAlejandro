package com.example.practicaprueba

import androidx.room.Dao
import androidx.room.Insert
import androidx.room.OnConflictStrategy
import androidx.room.Query
import com.example.practicaprueba.data.model.Factura

/*
@Dao
interface FacturasDao {

    @Query("SELECT ListaFactura * FROM tabla_NumFacturas ORDER BY NumeroFactura")
    suspend fun getFacturas(): List<FacturaEntity>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun insertAll(factura: NumFacturasEntity)


}*/
