package com.example.practicaprueba

import androidx.room.ColumnInfo
import androidx.room.Entity
import androidx.room.PrimaryKey
import com.example.practicaprueba.data.model.Factura
/*
@Entity(tableName = "tabla_NumFacturas")
data class NumFacturasEntity (
    @PrimaryKey(autoGenerate = true)
    @ColumnInfo(name = "id") val id: Int = 0,
    @ColumnInfo(name = "NumeroFactura")var numFacturas: Int,
    @ColumnInfo(name = "ListaFactura")var facturas: List<Factura>
    )*/
